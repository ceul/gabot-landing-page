name: Landing Page CI/CD

on:
  push:
    branches: [master, dev]

env:
  IMAGE: ghcr.io/ceul/gabot-landing

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      tag: ${{ steps.tag.outputs.value }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: tag
        run: |
          if [ "${{ github.ref_name }}" = "master" ]; then
            echo "value=${{ env.IMAGE }}:latest" >> "$GITHUB_OUTPUT"
          else
            echo "value=${{ env.IMAGE }}:dev" >> "$GITHUB_OUTPUT"
          fi

      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.tag.outputs.value }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          envs: DEPLOY_TAG,BRANCH,GH_TOKEN,GH_ACTOR
          script: |
            echo "$GH_TOKEN" | docker login ghcr.io -u "$GH_ACTOR" --password-stdin
            cd /opt/gabot
            docker pull "$DEPLOY_TAG"
            if [ "$BRANCH" = "master" ]; then
              docker compose --profile prod up -d --no-deps landing-prod
            else
              docker compose --profile dev up -d --no-deps landing-dev
            fi
        env:
          DEPLOY_TAG: ${{ needs.build-and-push.outputs.tag }}
          BRANCH: ${{ github.ref_name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_ACTOR: ${{ github.actor }}
